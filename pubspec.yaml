name: lasher_adani
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  lottie: ^3.1.2
  intl: ^0.19.0
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1       # For user authentication and OTP
  cloud_firestore: ^5.0.2    # For the database

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/LOGO.png
    - assets/neon_ring.json

